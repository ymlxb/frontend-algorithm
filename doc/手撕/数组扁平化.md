<!-- 采用扩展运算符和some的方法 -->
```js
let arr = [1, [2, [3, 4]]];
function flattenFnArray (arr) {
  while (arr.some(item => Array.isArray(item))) {
    arr =  [].concat(...arr)
  }
  return arr;
}
console.log(flattenFnArray(arr));

```